receivers:
  otlp:
    protocols:
      grpc:
      http:

processors:
  batch:

exporters:
  otlphttp:
    endpoint: "http://192.168.3.1:9998/e/1/api/v2/otlp"
    headers:
      Authorization: "Api-Token dt0c01.FAKESTAGXTJ4YBUPP3HACW6.FAKEH4TWMY76FAKEXLEON4WTNOBU437IFKNONT3WIXPK4EEA3ZUHURHOTFAKE"
  logging:
    loglevel: debug

extensions:
  health_check:
  pprof:
  zpages:

service:
  extensions: [health_check,pprof,zpages]
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [logging,otlphttp]
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [logging]
    logs:
      receivers: [otlp]
      processors: [batch]
      exporters: [logging]
